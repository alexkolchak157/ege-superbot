# Обязательные параметры
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Опциональные параметры
REQUIRED_CHANNEL=@your_channel  # Канал для проверки подписки
REMINDER_INACTIVITY_DAYS=3      # Дни неактивности для напоминаний
BOT_ADMIN_IDS=                 # Глобальные администраторы через запятую

# ============================================
# AI-провайдер для проверки заданий и OCR
# ============================================
# Выбор провайдера: "claude" (по умолчанию) или "yandex" (фоллбек)
# При проблемах с прокси переключить на Yandex: AI_PROVIDER=yandex
AI_PROVIDER=claude

# Anthropic API (Claude — для AI-проверки заданий и OCR)
# Используется при AI_PROVIDER=claude
ANTHROPIC_API_KEY=your_anthropic_api_key

# Прокси для Anthropic API (если API заблокирован по гео-признаку)
# Reverse proxy (Cloudflare Worker, nginx, etc.)
# Код Worker'а: proxy/cloudflare-worker.js
#ANTHROPIC_PROXY_URL=https://your-cf-worker.workers.dev
# ПРИМЕЧАНИЕ: через прокси Claude используется только для текстовых evaluator'ов.
# Vision OCR автоматически переключается на Yandex Vision (прокси не тянет большие payload'ы).
# Для Vision через Claude нужен прямой доступ к API (без прокси).

# Таймаут Claude Vision (секунды, по умолчанию 120)
#CLAUDE_VISION_TIMEOUT=120

# YandexGPT API (используется при AI_PROVIDER=yandex)
YANDEX_GPT_API_KEY=your_api_key
YANDEX_GPT_FOLDER_ID=your_folder_id

# Yandex Vision API (OCR при AI_PROVIDER=yandex)
# Использует те же ключи что и YandexGPT (YANDEX_GPT_API_KEY и YANDEX_GPT_FOLDER_ID)
# Опциональные настройки:
YANDEX_VISION_TIMEOUT=30        # Таймаут запроса в секундах (по умолчанию 30)
YANDEX_VISION_RETRIES=3         # Количество попыток при ошибке (по умолчанию 3)
YANDEX_VISION_RETRY_DELAY=2.0   # Задержка между попытками в секундах (по умолчанию 2.0)

# Администраторы task24
TASK24_ADMIN_IDS=123456789,987654321  # ID админов через запятую

# ============================================
# White-label / Брендинг (опционально)
# ============================================
# BOT_BRAND_NAME=ЕГЭ Супербот
# BOT_BRAND_SHORT_NAME=ЕГЭ Бот
# BOT_BRAND_DESCRIPTION=Подготовка к ЕГЭ по обществознанию с AI-проверкой
# BOT_BRAND_WELCOME=Привет! Я помогу тебе подготовиться к ЕГЭ.
# BOT_BRAND_SCHOOL_NAME=Название вашей школы
# BOT_BRAND_SUPPORT_CONTACT=support@school.ru
# BOT_BRAND_COLOR=#2196F3
# BOT_BRAND_LOGO_URL=https://school.ru/logo.png

