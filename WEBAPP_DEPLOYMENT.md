# Инструкция по развертыванию WebApp

Эта инструкция поможет развернуть Telegram WebApp для режима учителя на вашем сервере.

## 📋 Требования

- Ubuntu/Debian сервер с доступом по SSH
- Домен с настроенным DNS (у вас: обществонапальцах.рф → 193.124.112.38)
- Root доступ или sudo права
- Node.js 18+ (уже установлен: v20.19.6)

## 🚀 Шаги развертывания

### 1. Установка Nginx

```bash
sudo apt update
sudo apt install nginx -y
sudo systemctl enable nginx
sudo systemctl start nginx
```

Проверка установки:
```bash
nginx -v
sudo systemctl status nginx
```

### 2. Установка Certbot для SSL сертификатов

Telegram WebApp требует HTTPS соединения!

```bash
sudo apt install certbot python3-certbot-nginx -y
```

### 3. Получение SSL сертификата

**ВАЖНО:** Перед выполнением убедитесь, что:
- DNS записи для вашего домена указывают на IP сервера (193.124.112.38)
- Порты 80 и 443 открыты в файрволле

```bash
# Для IDN домена (кириллического) используем punycode версию
sudo certbot --nginx -d xn--80aaabfr9bnfdntn4cn1bzd.xn--p1ai

# Certbot автоматически:
# 1. Получит сертификат от Let's Encrypt
# 2. Настроит Nginx для использования HTTPS
# 3. Настроит автопродление сертификата
```

Выберите вариант: **2** (Redirect HTTP to HTTPS)

### 4. Копирование WebApp файлов на сервер

Если вы работаете локально, загрузите файлы на сервер:

```bash
# На локальной машине:
scp -r WebApp root@193.124.112.38:/opt/ege-bot/

# Или если уже на сервере:
# Файлы уже находятся в /home/user/ege-superbot/WebApp
# Нужно скопировать их в /opt/ege-bot/
sudo mkdir -p /opt/ege-bot
sudo cp -r /home/user/ege-superbot/WebApp /opt/ege-bot/
```

Проверьте структуру:
```bash
ls -la /opt/ege-bot/WebApp/teacher/
# Должны быть:
# - create-assignment.html
# - bundle.js
# - bundle.js.map
# - styles/
# - package.json
```

### 5. Настройка Nginx

Скопируйте конфигурацию:

```bash
sudo cp /home/user/ege-superbot/nginx_webapp.conf /etc/nginx/sites-available/webapp
sudo ln -s /etc/nginx/sites-available/webapp /etc/nginx/sites-enabled/
```

Проверьте конфигурацию:
```bash
sudo nginx -t
```

Если всё OK, перезагрузите Nginx:
```bash
sudo systemctl reload nginx
```

### 6. Настройка .env файла

`.env` файл уже создан и содержит правильный WEBAPP_URL:

```bash
cat /home/user/ege-superbot/.env | grep WEBAPP_URL
# Должно быть:
# WEBAPP_URL=https://xn--80aaabfr9bnfdntn4cn1bzd.xn--p1ai/WebApp/teacher/create-assignment.html
```

### 7. Проверка доступности WebApp

Откройте в браузере:
```
https://xn--80aaabfr9bnfdntn4cn1bzd.xn--p1ai/WebApp/teacher/create-assignment.html
```

Или используя кириллический домен (браузер автоматически конвертирует):
```
https://обществонапальцах.рф/WebApp/teacher/create-assignment.html
```

Вы должны увидеть страницу создания домашнего задания.

### 8. Перезапуск бота

Чтобы бот начал использовать новый WEBAPP_URL из .env:

```bash
# Если бот запущен как systemd service:
sudo systemctl restart ege-bot

# Или если бот запущен в screen/tmux:
# Найдите процесс и перезапустите его
```

## 🧪 Тестирование

1. **Откройте Telegram бота**
2. **Перейдите в режим учителя** (если вы зарегистрированы как учитель)
3. **Нажмите кнопку "🚀 Создать задание (WebApp)"**
4. **Должно открыться WebApp окно** с формой создания задания

### Возможные проблемы:

#### WebApp не открывается в Telegram
- Проверьте, что сайт доступен по HTTPS (откройте в браузере)
- Проверьте сертификат SSL (должен быть валидным)
- Проверьте, что WEBAPP_URL в .env правильный
- Перезапустите бота после изменения .env

#### Ошибка "Can't load page"
- Проверьте логи Nginx: `sudo tail -f /var/log/nginx/webapp_error.log`
- Проверьте права на файлы: `sudo chmod -R 755 /opt/ege-bot/WebApp`

#### CORS ошибки в консоли браузера
- Nginx конфигурация уже включает CORS headers
- Перезагрузите Nginx: `sudo systemctl reload nginx`

## 📱 Backend API (следующий шаг)

WebApp Frontend готов, но для полной функциональности нужно реализовать Backend API.

Смотрите файл `BACKEND_API_PROMPT.md` для детальных инструкций по созданию Backend.

Основные endpoints, которые нужно реализовать:

```
GET  /api/teacher/profile        - Профиль учителя
GET  /api/teacher/students       - Список учеников
GET  /api/teacher/modules        - Доступные модули
GET  /api/teacher/questions      - Вопросы по модулю
POST /api/teacher/assignments    - Создание задания
POST /api/teacher/drafts         - Сохранение черновика
GET  /api/teacher/drafts         - Получение черновиков
```

## 🔧 Полезные команды

### Просмотр логов Nginx
```bash
# Access logs
sudo tail -f /var/log/nginx/webapp_access.log

# Error logs
sudo tail -f /var/log/nginx/webapp_error.log
```

### Проверка статуса сервисов
```bash
sudo systemctl status nginx
sudo systemctl status certbot.timer
```

### Обновление Frontend после изменений
```bash
cd /home/user/ege-superbot/WebApp/teacher
npm run build
sudo cp bundle.js bundle.js.map /opt/ege-bot/WebApp/teacher/
```

### Тестирование SSL сертификата
```bash
openssl s_client -connect xn--80aaabfr9bnfdntn4cn1bzd.xn--p1ai:443 -servername xn--80aaabfr9bnfdntn4cn1bzd.xn--p1ai
```

## 📚 Дополнительные ресурсы

- [Telegram WebApp Documentation](https://core.telegram.org/bots/webapps)
- [Let's Encrypt Documentation](https://letsencrypt.org/docs/)
- [Nginx Documentation](https://nginx.org/en/docs/)

## ✅ Чеклист развертывания

- [ ] Nginx установлен и запущен
- [ ] SSL сертификат получен через Certbot
- [ ] WebApp файлы скопированы в /opt/ege-bot/WebApp
- [ ] Nginx конфигурация настроена и проверена
- [ ] WEBAPP_URL добавлен в .env
- [ ] WebApp доступен по HTTPS в браузере
- [ ] Бот перезапущен
- [ ] WebApp кнопка работает в Telegram
- [ ] Backend API реализован (см. BACKEND_API_PROMPT.md)

---

**Статус на данный момент:**
✅ Frontend готов и собран
✅ Конфигурация Nginx создана
✅ .env файл настроен
⏳ Требуется установка Nginx и получение SSL сертификата
⏳ Требуется реализация Backend API
