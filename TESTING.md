# Руководство по тестированию Teacher WebApp API

## Обзор

Этот документ содержит инструкции по тестированию интеграции Frontend WebApp с Backend API.

## Предварительные требования

- Backend API запущен на `https://обществонапальцах.рф`
- Nginx сконфигурирован и работает
- У вас есть доступ к Telegram боту в режиме учителя

## Доступные URL

### API Documentation
- **Swagger UI**: https://обществонапальцах.рф/docs
- **ReDoc**: https://обществонапальцах.рф/redoc
- **OpenAPI JSON**: https://обществонапальцах.рф/openapi.json
- **Health Check**: https://обществонапальцах.рф/health

### WebApp Interface
- **Teacher WebApp**: https://обществонапальцах.рф/WebApp/teacher/create-assignment.html

## 1. Тестирование API через Swagger UI

### 1.1 Проверка Health Endpoint

1. Откройте https://обществонапальцах.рф/docs
2. Найдите раздел **root** → `GET /health`
3. Нажмите "Try it out" → "Execute"
4. Проверьте ответ:
```json
{
  "status": "healthy",
  "service": "teacher-webapp-api"
}
```

### 1.2 Тестирование без аутентификации (ожидается ошибка 422)

Все endpoints требуют Telegram WebApp аутентификацию через заголовок `X-Telegram-Init-Data`.

1. Выберите любой endpoint, например `GET /api/teacher/modules`
2. Нажмите "Try it out" → "Execute"
3. Должна вернуться ошибка **422 Validation Error**:
```json
{
  "detail": [
    {
      "type": "missing",
      "loc": ["header", "X-Telegram-Init-Data"],
      "msg": "Field required"
    }
  ]
}
```

## 2. Тестирование через WebApp в Telegram

### 2.1 Открытие WebApp

1. Откройте бота в Telegram
2. Перейдите в режим учителя
3. Нажмите кнопку для создания задания
4. Откроется WebApp: https://обществонапальцах.рф/WebApp/teacher/create-assignment.html

### 2.2 Проверка аутентификации

WebApp автоматически:
- Извлечет `initData` из Telegram WebApp SDK
- Добавит заголовок `X-Telegram-Init-Data` ко всем API запросам
- Покажет данные профиля учителя

### 2.3 Тестирование функционала

#### Шаг 1: Загрузка списка учеников
- [ ] WebApp загрузился без ошибок
- [ ] Отображается список учеников
- [ ] Работает поиск по имени
- [ ] Работает выбор учеников

#### Шаг 2: Выбор модулей
- [ ] Загружается список доступных модулей
- [ ] Можно выбрать несколько модулей
- [ ] Для каждого модуля можно выбрать режим выбора вопросов (random/specific/all)

#### Шаг 3: Выбор вопросов
- [ ] При выборе "specific" открывается браузер вопросов
- [ ] Загружаются вопросы из выбранного модуля
- [ ] Работает поиск по вопросам
- [ ] Работает пагинация
- [ ] Можно выбрать конкретные вопросы

#### Шаг 4: Создание задания
- [ ] Заполнение названия и описания
- [ ] Выбор дедлайна (опционально)
- [ ] Превью задания перед отправкой
- [ ] Успешное создание задания
- [ ] Получение подтверждения

#### Шаг 5: Сохранение черновика
- [ ] Кнопка "Сохранить черновик" работает
- [ ] Черновик сохраняется
- [ ] Можно загрузить сохраненный черновик

## 3. Тестирование через Browser DevTools

### 3.1 Проверка Network запросов

1. Откройте WebApp в Telegram
2. Откройте DevTools (F12)
3. Перейдите на вкладку **Network**
4. Проверьте запросы к API:

#### GET /api/teacher/students
```
Status: 200 OK
Headers:
  X-Telegram-Init-Data: [присутствует]
Response:
{
  "total": 45,
  "students": [...]
}
```

#### GET /api/teacher/modules
```
Status: 200 OK
Response:
{
  "modules": [
    {
      "code": "test_part",
      "name": "📝 Тестовая часть (1-16)",
      "total_questions": 450,
      "description": "..."
    },
    ...
  ]
}
```

#### POST /api/teacher/assignments
```
Status: 200 OK
Request Body:
{
  "assignment_type": "existing_topics",
  "title": "Домашнее задание №5",
  "description": "...",
  "student_ids": [1, 2, 3],
  "modules": [...]
}
Response:
{
  "success": true,
  "assignment_id": 456,
  "students_notified": 3,
  "message": "..."
}
```

### 3.2 Проверка Console на ошибки

- [ ] Нет ошибок JavaScript
- [ ] Нет ошибок CORS
- [ ] Нет ошибок CSP
- [ ] Все ресурсы загружаются успешно

## 4. Тестирование граничных случаев

### 4.1 Валидация данных

#### Создание задания с невалидными данными:
- [ ] Пустое название → ошибка валидации
- [ ] Слишком длинное название (>100 символов) → ошибка
- [ ] Нет выбранных учеников → ошибка
- [ ] Нет выбранных модулей → ошибка
- [ ] Невалидный module_code → ошибка 422

### 4.2 Обработка ошибок

#### Сетевые ошибки:
- [ ] Потеря соединения → понятное сообщение об ошибке
- [ ] Timeout → обработка таймаута
- [ ] 500 Internal Server Error → понятное сообщение

#### Ошибки аутентификации:
- [ ] Невалидный initData → 401/403 ошибка
- [ ] Истекший initData → ошибка и предложение перезагрузить

### 4.3 Производительность

- [ ] Загрузка списка из 100+ учеников
- [ ] Загрузка 500+ вопросов с пагинацией
- [ ] Создание задания для 50+ учеников
- [ ] Сохранение большого черновика

## 5. Проверка безопасности

### 5.1 Content Security Policy
- [ ] CSP заголовки правильно настроены
- [ ] Внешние скрипты загружаются только с telegram.org
- [ ] Нет inline scripts (кроме разрешенных)

### 5.2 HTTPS
- [ ] Все запросы идут через HTTPS
- [ ] Нет mixed content предупреждений
- [ ] SSL сертификат валидный

### 5.3 Аутентификация
- [ ] Невозможно обойти Telegram WebApp аутентификацию
- [ ] initData валидируется на backend
- [ ] Подпись HMAC проверяется корректно

## 6. Мониторинг и логи

### 6.1 Проверка логов API

```bash
# На сервере
sudo journalctl -u teacher-api -f
```

Ожидаемые логи:
```
INFO:     127.0.0.1:XXXXX - "GET /api/teacher/students HTTP/1.0" 200 OK
INFO:     127.0.0.1:XXXXX - "GET /api/teacher/modules HTTP/1.0" 200 OK
INFO:     127.0.0.1:XXXXX - "POST /api/teacher/assignments HTTP/1.0" 200 OK
```

### 6.2 Nginx логи

```bash
sudo tail -f /var/log/nginx/webapp_access.log
sudo tail -f /var/log/nginx/webapp_error.log
```

## 7. Чек-лист готовности к продакшену

- [ ] API endpoints работают корректно
- [ ] WebApp загружается без ошибок
- [ ] Аутентификация работает
- [ ] Все CRUD операции функционируют
- [ ] Обработка ошибок корректная
- [ ] Производительность приемлемая
- [ ] Безопасность проверена
- [ ] Логирование настроено
- [ ] Мониторинг работает
- [ ] Документация актуальна

## Известные ограничения

1. **Тестирование только в Telegram WebApp**: API требует валидный initData от Telegram, поэтому тестирование через Swagger UI ограничено.

2. **CORS**: API принимает запросы только от Telegram доменов (t.me, web.telegram.org).

3. **Rate Limiting**: Пока не настроен, может потребоваться в будущем.

## Отчет о проблемах

Если вы обнаружили проблему:
1. Запишите URL и действия для воспроизведения
2. Сделайте скриншот ошибки
3. Скопируйте логи из DevTools Console и Network
4. Создайте issue в репозитории

## Дополнительные ресурсы

- API Documentation: https://обществонапальцах.рф/docs
- Backend API Spec: см. BACKEND_API_PROMPT.md
- Deployment Guide: см. DEPLOYMENT.md
